# Required
VAULT_REMOTE=https://github.com/youruser/octopal-vault.git

# Daemon admin password (used by `octopal serve --set-password` and token provisioning)
OCTOPAL_PASSWORD=changeme

# code-server password (protects the web UI in the browser)
CODE_SERVER_PASSWORD=changeme

# Web viewer URL — how users reach code-server from their browser
# Default: http://localhost:8443 (local dev)
# With Traefik: https://vault.yourdomain.com
VAULT_BASE_URL=http://localhost:8443

# GitHub PAT — shared by gh CLI (vault git ops) and Copilot SDK (AI)
# Generate at: https://github.com/settings/tokens
GH_TOKEN=

# Optional: custom ports
# OCTOPAL_PORT=3847
# CODE_SERVER_PORT=8443

# Optional: Discord connector
# DISCORD_BOT_TOKEN=
# DISCORD_ALLOWED_USERS=user1,user2
# DISCORD_CHANNELS=channel1,channel2

# Optional: Traefik (only with docker-compose.traefik.yml)
# DOMAIN=yourdomain.com
# ACME_EMAIL=you@example.com
