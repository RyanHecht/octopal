# Triage Queue

Review pending associations the agent wasn't sure about. For each item:
- Check the box ✅ to approve (alias will be added to the knowledge entry)
- Mark with `[-]` ❌ to reject (link will be removed)
- Edit the suggestion if it's close but not quite right

Run `octopal triage` to process your decisions.

## Pending

## Processed
